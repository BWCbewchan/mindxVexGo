# Custom field appearance

Implement visible image upload with compression and decoding errors; preserve background through JSON. Add square/circle/triangle/hexagon cargo rendering and validated HEX colors for blocks and zones. Keep the existing 40 mm collision envelope. Verify upload, reload, export and runtime pushing; build without pushing.
