import type {Metadata} from 'next';
export const metadata:Metadata={title:'Classroom · mindX',description:'Celebrate student progress with classroom points, companions and rewards.'};
export default function Layout({children}:{children:React.ReactNode}){return children;}
