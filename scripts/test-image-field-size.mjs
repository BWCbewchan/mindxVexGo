import {chromium} from 'playwright';import assert from 'node:assert/strict';const b=await chromium.launch({channel:'chrome'});try{const p=await b.newPage();await p.goto('http://localhost:3117/training/custom?new=1');await p.locator('.designer-canvas').waitFor();const png=await p.evaluate(()=>{const c=document.createElement('canvas');c.width=1600;c.height=800;return c.toDataURL().split(',')[1];});await p.getByLabel('Background image',{exact:true}).setInputFiles({name:'wide.png',mimeType:'image/png',buffer:Buffer.from(png,'base64')});await p.waitForFunction(()=>document.querySelector('.designer-canvas')?.getAttribute('viewBox')==='0 0 1600 800');assert.equal(await p.locator('.designer-canvas image').getAttribute('width'),'1600');assert.equal(await p.locator('.designer-canvas image').getAttribute('height'),'800');await p.reload();await p.waitForFunction(()=>document.querySelector('.designer-canvas')?.getAttribute('viewBox')==='0 0 1600 800');console.log('PASS: image dimensions set field and background to 1600x800 and survive reload.');}finally{await b.close();}
