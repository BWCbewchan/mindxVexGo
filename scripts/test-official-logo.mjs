import {chromium} from 'playwright';import assert from 'node:assert/strict';const b=await chromium.launch({channel:'chrome'});try{const p=await b.newPage({viewport:{width:1440,height:900}});for(const path of ['/','/training','/training/custom?new=1']){await p.goto('http://localhost:3117'+path);await p.locator('.mindx-official-logo img').waitFor();const box=await p.locator('.mindx-official-logo').first().boundingBox();assert.ok(box.width>60&&box.height>=25);assert.equal((await p.locator('.mindx-official-logo img').first().getAttribute('src')).startsWith('/mindx-logo-'),true);}await p.goto('http://localhost:3117/studio');const frame=await (await p.locator('iframe').elementHandle()).contentFrame();await frame.waitForSelector('.editor-mindx-logo img');assert.equal(await frame.locator('.studio-home').textContent(),'/ GO');await p.goto('http://localhost:3117/');await p.screenshot({path:'artifacts/official-logo-home.png'});console.log('PASS: official logo visible on home, training, custom field and Blockly Studio header.');}finally{await b.close();}
