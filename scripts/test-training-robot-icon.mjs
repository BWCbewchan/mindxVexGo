import {chromium} from 'playwright';import assert from 'node:assert/strict';const b=await chromium.launch({channel:'chrome'});try{const p=await b.newPage();await p.goto('http://localhost:3100/training');assert.ok(await p.locator('.lesson-preview image[href="/training-robot.png"]').count()>0);await p.goto('http://localhost:3100/training/custom?new=1');await p.locator('[data-kind="start"] image').waitFor();assert.equal(await p.locator('[data-kind="start"] image').getAttribute('href'),'/training-robot.png');assert.equal((await p.request.get('http://localhost:3100/training-robot.png')).status(),200);await p.screenshot({path:'artifacts/training-robot-new.png'});console.log('PASS: robot asset available; gallery, designer and layer thumbnail use new robot.');}finally{await b.close();}
