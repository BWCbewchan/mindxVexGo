import {chromium} from 'playwright';import assert from 'node:assert/strict';const b=await chromium.launch({channel:'chrome'});try{const p=await b.newPage();await p.goto('http://localhost:3100/');for(const width of [1440,768,390]){await p.setViewportSize({width,height:900});assert.equal(await p.locator('.workspace-card').count(),2);assert.ok(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));}assert.equal(await p.locator('.workspace-studio').getAttribute('href'),'/studio');assert.equal(await p.locator('.workspace-training').getAttribute('href'),'/training');await p.screenshot({path:'artifacts/home-hub-mobile.png'});await p.setViewportSize({width:1440,height:1000});await p.screenshot({path:'artifacts/home-hub-desktop.png'});console.log('PASS: Studio/Training entry points and responsive layout at 1440, 768 and 390px.');}finally{await b.close();}
