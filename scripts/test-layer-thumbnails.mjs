import {chromium} from 'playwright';import assert from 'node:assert/strict';const b=await chromium.launch({channel:'chrome'});try{const p=await b.newPage({viewport:{width:1440,height:1000}});await p.goto('http://localhost:3126/training/custom');await p.getByRole('button',{name:'Add push block',exact:true}).click();await p.getByRole('button',{name:'Add push block',exact:true}).click();const group=p.locator('.designer-layer-group').filter({has:p.getByRole('button',{name:'Select blocks layer',exact:true})});await group.locator('.layer-objects summary').click();await p.getByRole('button',{name:'Select blocks object 2',exact:true}).click();assert.equal(await p.locator('[data-resize]').getAttribute('data-index'),'1');assert.equal(await group.locator('.layer-preview svg').count(),3);await p.getByRole('button',{name:'Hide blocks layer',exact:true}).click();assert.equal(await p.locator('[data-kind="blocks"]').count(),0);await p.screenshot({path:'artifacts/layers-thumbnails.png'});console.log('PASS: thumbnails, individual selection and layer visibility.');}finally{await b.close();}
