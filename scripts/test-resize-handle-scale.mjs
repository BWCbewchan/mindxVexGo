import {chromium} from 'playwright';import assert from 'node:assert/strict';const b=await chromium.launch({channel:'chrome'});try{const p=await b.newPage();await p.goto('http://localhost:3117/training/custom?new=1');await p.getByRole('button',{name:'Add wall',exact:true}).click();for(let i=0;i<8;i++){await p.getByRole('button',{name:'Zoom in',exact:true}).click();const v=await p.locator('.resize-handle-visual').boundingBox(),hit=await p.locator('.resize-handle').boundingBox();console.log(v.width,hit.width);assert.ok(Math.abs(v.width-14)<.5);assert.ok(Math.abs(hit.width-24)<.5);}await p.getByRole('button',{name:'Fit field',exact:true}).click();for(let i=0;i<4;i++){await p.getByRole('button',{name:'Zoom out',exact:true}).click();assert.ok(Math.abs((await p.locator('.resize-handle-visual').boundingBox()).width-14)<.5);}console.log('PASS: 12px visual / 24px hit area across 10–300% zoom.');}finally{await b.close();}
